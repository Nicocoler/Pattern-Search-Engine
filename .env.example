# 复制为服务器上的 .env（勿提交真实密码）
# 容器内访问宿主机上已运行的 PostgreSQL 时，主机名用 host.docker.internal
# 若 PG 在同一 Docker 自定义网络，可改为该容器名，并去掉 compose 里 extra_hosts 依赖

DATABASE_URL=postgresql://USER:PASSWORD@host.docker.internal:5432/DATABASE_NAME

# 布林编排全市场扫描线程数（2 核+同机 PG 建议 2～4，默认 3；代码侧硬夹 1～4）
# BOLL_SCAN_WORKERS=3